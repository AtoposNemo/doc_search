# 文档搜索工具 — 部署说明

## 一、产品概述

**文档搜索工具** 是一款本地运行的文档全文检索软件，支持在文件夹中搜索关键词，并高亮显示匹配内容。

### 核心特性

- 🔍 **多格式支持**：`.doc` `.docx` `.txt` `.csv` `.xlsx` `.xls` `.pdf`（共 7 种）
- 🌐 **中英文搜索**：自动识别中英文，关键词高亮显示
- 📄 **上下文摘要**：以关键词为中心，前后各 200 字符上下文
- 🔒 **代码保护**：Cython 编译为原生机器码，源码不可逆向
- 🖥️ **本地运行**：无需联网，无需安装 Python，单文件即可运行
- 🔐 **安全隔离**：COM 自动化使用独立进程，不干扰用户已打开的 Office 文档

---

## 二、交付物

### 只需发送一个文件

| 文件 | 大小 | 说明 |
|------|------|------|
| **文档搜索.exe** | ~34 MB | 完整独立程序，双击即可运行 |

> 该文件已通过 PyInstaller `--onefile` 模式打包，内置 Python 运行时和所有依赖库，**无需额外安装任何软件即可运行**（前提条件见下文）。

---

## 三、系统要求

### 操作系统

| 系统 | 支持 | 备注 |
|------|------|------|
| Windows 11 | ✅ 完全支持 | — |
| Windows 10 | ✅ 完全支持 | — |
| Windows 8.1 | ✅ 支持 | 需安装 UCRT 运行库 |
| Windows 7 | ⚠️ 部分支持 | 需安装 KB2999226 更新和 UCRT 运行库 |

### Office 软件（按搜索需求）

| 搜索文件类型 | 需要安装 | 说明 |
|-------------|---------|------|
| `.docx` `.txt` `.csv` `.xlsx` `.pdf` | ❌ 无需安装 | 纯 Python 库解析，开箱即用 |
| `.doc`（旧版 Word） | ✅ 需安装以下之一 | 通过 COM 接口读取 |
| `.xls`（旧版 Excel） | ✅ 需安装以下之一 | 通过 COM 接口读取 |

可选的 Office 软件（任选其一即可）：

| 软件 | 支持 .doc | 支持 .xls | 备注 |
|------|----------|----------|------|
| Microsoft Word + Excel | ✅ | ✅ | 优先使用 |
| WPS Office | ✅ | ✅ | 自动回退兼容 |

> **提示**：如果客户只搜索 `.docx` 和 `.xlsx` 等新格式文件，则**无需安装任何 Office 软件**。程序会自动检测，未安装时给出友好提示。

### Visual C++ 运行库

- Windows 10/11 通常已预装
- Windows 7/8 可能需要安装 [Visual C++ Redistributable](https://aka.ms/vs/17/release/vc_redist.x64.exe)

---

## 四、使用说明

### 4.1 安装与启动

1. 将 `文档搜索.exe` 复制到电脑任意位置（如桌面）
2. 双击 `文档搜索.exe` 启动程序
3. 等待 3-5 秒，程序窗口自动打开

### 4.2 搜索操作

1. **输入关键词**：在搜索框中输入要查找的关键词（中文或英文）
2. **选择文件夹**：点击「浏览...」按钮，选择包含文档的文件夹
   - 程序会递归搜索所选文件夹及所有子文件夹
3. **区分大小写**（可选）：勾选「区分大小写」复选框
4. **开始搜索**：点击「开始搜索」按钮
5. **查看结果**：
   - 搜索结果显示匹配的文件列表
   - 点击文件名左侧箭头展开，查看关键词上下文摘要
   - 关键词以高亮显示

### 4.3 文件类型校验

搜索前，程序会自动扫描文件夹中的所有文件类型：

- ✅ 如果文件夹中**只包含**支持的格式（`.doc` `.docx` `.txt` `.csv` `.xlsx` `.xls` `.pdf`），正常搜索
- ❌ 如果文件夹中**包含不支持的格式**（如 `.jpg` `.mp4` 等），弹出错误提示，列出具体的不符合文件名，并终止搜索

> 请确保所选文件夹中只包含上述 7 种格式的文件。

---

## 五、依赖项清单

### 5.1 运行时依赖（最终用户）

最终用户**无需安装任何 Python 相关软件**。以下依赖已全部打包进 exe：

| 依赖 | 版本 | 用途 | 已打包 |
|------|------|------|--------|
| Python | 3.8.18 | 运行时解释器 | ✅ |
| tkinter | 内置 | GUI 界面框架 | ✅ |
| python-docx | ≥ 1.1.0 | 解析 .docx 文件 | ✅ |
| openpyxl | ≥ 3.1.0 | 解析 .xlsx 文件 | ✅ |
| PyPDF2 | ≥ 3.0.0 | 解析 .pdf 文件 | ✅ |
| pywin32 | ≥ 306 | COM 自动化（.doc/.xls） | ✅ |
| Cython | 3.2.9 | 源码编译保护（仅开发时） | ❌ 不打包 |

### 5.2 开发依赖（开发者）

如需重新编译或修改代码，需要以下环境：

| 工具 | 版本 | 用途 |
|------|------|------|
| Python | 3.8.18 | Win7 兼容性要求（通过 Conda 安装） |
| Cython | 3.2.9 | 将 .py 编译为 .pyd 原生机器码 |
| PyInstaller | ≥ 6.0 | 将 .pyd + 依赖打包为单一 exe |
| MSVC Build Tools | 2022 | C 编译器（Cython 编译需要） |
| Windows SDK | 10.0.26100.0 | C 编译头文件和库 |

### 5.3 开发依赖安装命令

```bash
# 创建 Python 3.8 环境（Anaconda）
conda create -n py38 python=3.8.18
conda activate py38

# 安装 Python 依赖
pip install python-docx openpyxl PyPDF2 pywin32 cython pyinstaller
```

---

## 六、代码保护说明

### 保护架构

```
源码 (.py)  →  Cython 编译  →  原生机器码 (.pyd)  →  PyInstaller 打包  →  最终 exe
```

### 保护措施

| 层级 | 措施 | 效果 |
|------|------|------|
| 源码编译 | Cython 将 Python 转为 C 代码，编译为 .pyd | 不可用 uncompyle6 等工具反编译 |
| 源码移除 | .py 文件不打包进 exe | exe 中不含任何 Python 源码 |
| 调试剥离 | `strip=True` + `emit_code_comments=False` | 无注释、无调试符号 |
| 签名隐藏 | `embedsignature=False` | 不暴露函数名和参数 |
| UPX 压缩 | `upx=True` | exe 压缩，增加分析难度 |

### 重新编译流程（开发者）

```bash
# 1. 将源码复制到 protected_build 目录
copy main_gui.py protected_build\
copy search_docs.py protected_build\

# 2. 运行 Cython 编译脚本
cd protected_build
python setup_cython.py

# 3. 删除 .py 源文件和中间文件（保留 .pyd）
del main_gui.py search_docs.py main_gui.c search_docs.c setup_cython.py
rmdir /s /q build

# 4. PyInstaller 打包
python -m PyInstaller protected_build.spec --distpath .. --workpath build

# 5. 清理临时文件
rmdir /s /q build
```

---

## 七、常见问题

### Q1：双击 exe 后没有反应？

**A**：程序启动需要 3-5 秒加载时间。如果超过 10 秒仍无反应，可能是杀毒软件拦截，请将 exe 添加到信任列表。

### Q2：提示「缺少 Office 软件」？

**A**：说明文件夹中包含 `.doc` 或 `.xls` 格式文件，需要安装 Microsoft Office 或 WPS Office。如果只需搜索 `.docx` / `.xlsx` / `.txt` / `.csv` / `.pdf`，则无需安装。

### Q3：提示「文件类型错误」？

**A**：文件夹中包含不支持的文件格式。支持的格式为：`.doc` `.docx` `.txt` `.csv` `.xlsx` `.xls` `.pdf`。请移除其他格式文件后重试。

### Q4：搜索速度慢？

**A**：`.docx` `.txt` `.csv` `.xlsx` `.pdf` 文件使用纯 Python 库并行搜索，速度较快。`.doc` 和 `.xls` 文件需要通过 COM 接口逐个打开，速度较慢。建议将旧格式转换为新格式以提升搜索速度。

### Q5：搜索时 Word/Excel 窗口弹出？

**A**：本程序已使用 `DispatchEx` 创建独立的 COM 进程实例，不会干扰用户已打开的 Office 文档。如果仍有弹出，请确保使用最新版本的 exe。

### Q6：Windows 7 无法运行？

**A**：Windows 7 需要安装以下更新：
1. [KB2999226](https://support.microsoft.com/kb/2999226)（通用 C 运行库）
2. [Visual C++ Redistributable 2015-2022](https://aka.ms/vs/17/release/vc_redist.x64.exe)

### Q7：界面文字模糊？

**A**：程序已内置 DPI 适配，在高分屏上自动调整清晰度。如仍模糊，请右键 exe → 属性 → 兼容性 → 勾选「替代高 DPI 缩放行为」→ 选择「应用程序」。

---

## 八、文件清单

### 发布给用户的文件

```
文档搜索.exe          (~34 MB)   唯一需要交付的文件
```

### 源码目录结构（开发者）

```
doc_search/
├── 文档搜索.exe              最终发布 exe
├── main_gui.py               GUI 主程序源码
├── search_docs.py            搜索引擎源码
├── requirements.txt          Python 依赖清单
├── doc_search_gui.spec       PyInstaller 打包配置（开发版）
├── .gitignore                Git 忽略规则
├── 部署说明.md                本文档
├── protected_build/          代码保护编译目录
│   ├── launcher.py           极简启动入口（352 字节，无核心逻辑）
│   ├── main_gui.cp38-win_amd64.pyd    GUI 编译后原生机器码
│   ├── search_docs.cp38-win_amd64.pyd 搜索引擎编译后原生机器码
│   └── protected_build.spec  保护版打包配置
└── files/                    测试文档目录
    └── test_formats/         多格式测试文件
        ├── 测试文档.txt
        ├── 数据表.csv
        ├── 工作簿.xlsx
        └── 技术报告.pdf
```

---

## 九、版本信息

| 项目 | 内容 |
|------|------|
| 程序名称 | 文档搜索工具 |
| 版本 | 1.0 |
| 打包模式 | PyInstaller --onefile（单文件） |
| 代码保护 | Cython 编译为 .pyd 原生机器码 |
| Python 版本 | 3.8.18（Windows 7 兼容） |
| 支持格式 | .doc .docx .txt .csv .xlsx .xls .pdf |
| 界面框架 | tkinter（桌面原生 GUI） |
| 目标系统 | Windows 7 / 8.1 / 10 / 11 |
